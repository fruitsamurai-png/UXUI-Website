// Get the button element by its id
const myButton = document.getElementById('schedule');

// Add a click event listener to the button
myButton.addEventListener('click', function() {
  // Navigate to another page
  console.log("hello");
});